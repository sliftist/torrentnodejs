node_modules
build
dist
**/dist
yarn-error.log
*.log
downloads
*.mp4
*.mkv

# Generated test seed data (large random blobs).
seed-data/
*.bin

# Debug artifacts (e.g. generated trackerless .torrents for peer testing).
debug/

# Stress-test corpora fetched by scripts/fetchCorpus.ts.
test-corpus/
corpus-*/
*-corpus/

# CLI runtime config — may contain local paths to WireGuard credentials.
bittorrent.config.yaml
# CLI runtime state (pause flags, etc.)
bittorrent.state.json
